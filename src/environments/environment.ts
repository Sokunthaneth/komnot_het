// This file can be replaced during build by using the `fileReplacements` array.
// `ng build --prod` replaces `environment.ts` with `environment.prod.ts`.
// The list of file replacements can be found in `angular.json`.

export const environment = {
  production: false,
  firebase: {
    apiKey: "AIzaSyC1fQHV5Bm6vlFajidxUjwT7refzI2y_Eo",
    authDomain: "komnot-het.firebaseapp.com",
    databaseURL: "https://komnot-het.firebaseio.com",
    projectId: "komnot-het",
    storageBucket: "komnot-het.appspot.com",
    messagingSenderId: "130771261573",
    appId: "1:130771261573:web:456bc59cd7caca45"
  }
};

/*
 * For easier debugging in development mode, you can import the following file
 * to ignore zone related error stack frames such as `zone.run`, `zoneDelegate.invokeTask`.
 *
 * This import should be commented out in production mode because it will have a negative impact
 * on performance if an error is thrown.
 */
// import 'zone.js/dist/zone-error';  // Included with Angular CLI.
